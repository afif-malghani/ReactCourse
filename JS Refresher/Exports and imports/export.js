export const add = (a,b) => a+b;
//  for single export we can define a function and then export it with:
//  "export default add", whereas for multi export there can only be one "default";
export const baseData = 10;

export default add;